spring:
  datasource:
    url: jdbc:h2:tcp://quotes_db:1521/votes
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: update
server:
  port: 8081
